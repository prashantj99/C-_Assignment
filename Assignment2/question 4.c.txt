#include <stdio.h>

int main(void) {
// 	swap two numbers (without third variable)
    int a,b;
    scanf("%d %d ",&a,&b);
    
    printf("Before Swap : a=%d ,b=%d\n",a,b); 
    
    a=a^b;
    b=a^b;
    a=a^b;
    
    printf("After swap  : a=%d ,b=%d",a,b);
	return 0;
}

