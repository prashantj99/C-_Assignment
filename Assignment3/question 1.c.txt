#include <stdio.h>

int main(void) {
     /*to check whether a given number 
     is positive or non-positive.*/ 
	 int n;
	 scanf("%d",&n);
	 
	 if(n>0)
	 {
	     printf("Positive");
	 }
	 else
	 {
	     printf("Non-Positive");
	 }
	 return 0;
}

